{
	"name": "@waf-checker/worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"build": "echo 'No build step for worker'"
	},
	"dependencies": {
		"@waf-checker/core": "*"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.22.0",
		"@vitest/coverage-v8": "~4.1.10",
		"typescript": "^7.0.2",
		"vitest": "~4.1.10",
		"wrangler": "^4.127.0"
	}
}
