{
  "name": "@waf-checker/cli",
  "version": "1.3.0",
  "type": "module",
  "bin": "./dist/index.js",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --external:commander --external:undici",
    "test": "vitest run"
  },
  "dependencies": {
    "@waf-checker/core": "*",
    "commander": "^15.0.0",
    "undici": "^8.10.2"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "typescript": "^7.0.2"
  }
}
