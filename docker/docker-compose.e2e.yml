services:
  waf-test-backend:
    image: nginx:alpine
    container_name: waf-test-backend
    ports:
      - "8081:80"
    networks:
      - waf-e2e-net

  waf-test-modsec:
    image: owasp/modsecurity-crs:nginx
    container_name: waf-test-modsec
    environment:
      - BACKEND=http://waf-test-backend:80
      - PARANOIA=1
      - ANOMALY_INBOUND=5
    ports:
      - "8088:8080"
    depends_on:
      - waf-test-backend
    networks:
      - waf-e2e-net

  waf-test-caddy:
    image: caddy:alpine
    container_name: waf-test-caddy
    volumes:
      - ./caddy:/etc/caddy:ro
    ports:
      - "8089:80"
    depends_on:
      - waf-test-backend
    networks:
      - waf-e2e-net

  waf-test-haproxy:
    image: haproxy:alpine
    container_name: waf-test-haproxy
    volumes:
      - ./haproxy/haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    ports:
      - "8090:80"
    depends_on:
      - waf-test-backend
    networks:
      - waf-e2e-net

  waf-test-nginx:
    image: nginx:alpine
    container_name: waf-test-nginx
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "8091:80"
    depends_on:
      - waf-test-backend
    networks:
      - waf-e2e-net

  waf-test-apache:
    image: httpd:2.4-alpine
    container_name: waf-test-apache
    volumes:
      - ./apache/httpd.conf:/usr/local/apache2/conf/httpd.conf:ro
    ports:
      - "8092:80"
    depends_on:
      - waf-test-backend
    networks:
      - waf-e2e-net

  waf-test-envoy:
    image: envoyproxy/envoy:v1.31-latest
    container_name: waf-test-envoy
    volumes:
      - ./envoy/envoy.yaml:/etc/envoy/envoy.yaml:ro
    ports:
      - "8093:80"
    depends_on:
      - waf-test-backend
    networks:
      - waf-e2e-net

networks:
  waf-e2e-net:
    driver: bridge
